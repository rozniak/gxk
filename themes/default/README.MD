# Default theme
This is a split up version of the upstream GTK 4 default theme (Adwaita). Rather than having everything in a few massive SASS documents, the definitions are separated out into smaller chunks.

## Building
Just use `sass` like so:
```
sass main.scss gxk.css
```
